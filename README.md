# Neurolog2
git clone https://github.com/vertexcyberneticsceo-collab/neurolog.git
cd neurolog

mkdir backend mobile nginx
touch README.md .gitignore docker-compose.yml

git add .
git commit -m "Initial Neurolog project"
git push -u origin main